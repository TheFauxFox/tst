TST - Tabbed Simple Terminal
============================

A hacked together mod of Suckless's tabbed and st to make a tabbed terminal
that sucks less. The root project is tabbed, renamed to tst, with st being built
as a library in the background.

To build you will need to install
    libharfbuzz-dev libfontconfig1-dev libfreetype-dev libx11-dev libxft-dev libbz2-dev

